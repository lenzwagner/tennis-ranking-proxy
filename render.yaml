services:
  - type: web
    name: tennis-ranking-proxy
    runtime: node
    nodeVersion: 20.11.0
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
